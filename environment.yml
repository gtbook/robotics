name: gtbook
channels:
  - conda-forge
dependencies:
  - python=3.7
  - jupyter-book=0.12.1
  - python-graphviz
  - ipywidgets>=7.5
  - pandas
  - pip
  - pip:
      - plotly==5.5.0 # in pip for binder
      - gtbook
