name: gtbook
channels:
  - conda-forge
dependencies:
  - python=3.7
  - jupyter-book
  - pip
  - python-graphviz
  - ipywidgets>=7.5
  - pandas
  - pip:
      - plotly # in pip for binder
      - gtbook
